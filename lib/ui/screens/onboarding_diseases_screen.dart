import 'package:flutter/material.dart';
class OnboardingDiseasesScreen extends StatelessWidget {
  const OnboardingDiseasesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}